version: "3.1"

services:
  client:
    build: "./client"
    container_name: "image-gallery-client"
    restart: always
    ports:
      - "3000:3000"
    networks:
      - image-gallery
    depends_on:
      - envoy

  envoy:
    build: "./envoy"
    container_name: "image-gallery-envoyproxy"
    restart: always
    ports:
      - "8080:8080"
    networks:
      - image-gallery
    depends_on:
      - server

  server:
    build: "."
    container_name: "image-gallery-server"
    restart: always
    ports:
      - "5000:5000"
    networks:
      - image-gallery
    depends_on:
      - psdb

  psdb:
    image: postgres
    container_name: "image-gallery-psdb"
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: 12345678
      POSTGRES_USER: "postgres"
      POSTGRES_DB: "image-gallery"
    networks:
      - image-gallery

networks:
  image-gallery:
    driver: bridge