syntax = "proto3";

package greeting;
option go_package = "/protobuf";

message Request {
    string msg = 1;
}

message Response {
    string result = 1;
}

service Greeting {
    rpc Login(Request) returns (Response) {};
}